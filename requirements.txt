fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
# Production database driver. Railway's Postgres plugin sets WECOM_DATABASE_URL to
# a `postgresql+psycopg2://` URL; SQLAlchemy resolves that dialect at engine-creation
# time, which requires the driver to be present in the image. Without it, the app
# import crashes before uvicorn binds to PORT and Railway reports
# "Application failed to respond". `psycopg2-binary` ships its own libpq, so it
# works on a clean Nixpacks build with no extra system packages.
psycopg2-binary>=2.9
pydantic>=2.7
pydantic-settings>=2.3
httpx>=0.27
cryptography>=42.0
python-multipart>=0.0.9
pytest>=8.0
