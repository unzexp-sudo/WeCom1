# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# Secrets (keep .env.example tracked)
.env

# Runtime data: SQLite DB, media, mock archives, outbox
data/

# OS
.DS_Store

# WeCom finance SDK binary + Session-Archive RSA private key — never commit.
# The .so is fetched at boot from WeCom's CDN (see services/sdk_bootstrap.py);
# the key is supplied via WECOM_ARCHIVE_PRIVATE_KEY_B64 or mounted at runtime.
# If you drop them locally for testing, keep them out of git.
vendor/
libWeWorkFinanceSdk.so
libWeWorkFinanceSdk_C.so
archive_private_key.pem
*.pem
